[project]
name = "albion-torch"
version = "0.1.0"
description = "Generates class information from compiled Java files"
requires-python = ">=3.12"
dependencies = [
    "kirjava-jvm",
    "yamlcore==0.0.4",
]

[tool.uv.sources]
kirjava-jvm = { path = "lib/kirjava", editable = true }

